\environment prenv
% prenv.tex is the environment file that
% sets up the layout, fonts, and style. It
% also loads the filter module and sets up
% a |markdown| external filter environment.

\starttext
  \processmarkdownfile{cas_validate_2.md}
\stoptext

%% \processmarkdownfile{\inputfile}
